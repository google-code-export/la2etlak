{"<%= @stories.class %>":
{
<% @stories.each do |story| %>
"id" :  "<%= story.id %>"
"title" : "<%= story.title %>"
"body" : "<%= story.content %>"
"rank" : "<%= story.rank %>"
"image" : "<%= story.media_link %>"
"category" : "<%= story.category %>"

<%end%>
}}

